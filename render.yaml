services:
  - type: web
    name: authenx-ai-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn authenx_ai_service:app --host 0.0.0.0 --port 10000"
    plan: free
